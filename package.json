{
  "name": "webbprojekt",
  "version": "1.0.0",
  "scripts": {
    "build": "sass src:dist --no-source-map --style=expanded",
    "watch": "sass src:dist --watch --style=expanded"
  },
  "devDependencies": {
    "sass": "^1.69.0"
  }
}